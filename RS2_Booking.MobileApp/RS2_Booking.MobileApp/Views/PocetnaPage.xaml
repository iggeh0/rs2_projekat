<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ViewModels="clr-namespace:RS2_Booking.MobileApp.ViewModels"
             mc:Ignorable="d"
             x:Class="RS2_Booking.MobileApp.Views.PocetnaPage"
             Title="Pretraga smještaja">
    <ContentPage.BindingContext>
        <ViewModels:PocetnaVM></ViewModels:PocetnaVM>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Odaberite način sortiranja: "></Label>
            <Picker ItemsSource="{Binding Sorteri}" SelectedItem="{Binding SelectedSort}"></Picker>
            <Label Text="Odaberite željeni grad: "></Label>
            <Picker ItemsSource="{Binding ListaGradova}" ItemDisplayBinding="{Binding Naziv}" SelectedItem="{Binding SelectedGrad}"></Picker>
            <ListView ItemsSource="{Binding ListaSmjestaja}" ItemTapped="ListView_ItemTapped" SelectedItem="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10">
                                <Label Text="{Binding Naziv}" 
                                LineBreakMode="NoWrap" 
                                Style="{DynamicResource ListItemTextStyle}" 
                                FontSize="16" />
                                <Label Text="{Binding GradNaziv}" 
                                LineBreakMode="NoWrap"
                                Style="{DynamicResource ListItemDetailTextStyle}"
                                FontSize="13" />
                                <Label IsVisible="{Binding Distanca.HasValue}"  LineBreakMode="NoWrap" 
                                Style="{DynamicResource ListItemTextStyle}" 
                                FontSize="13" >
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span>Udaljenost od centra: </Span>
                                            <Span Text="{Binding Distanca}"></Span>
                                            <Span Text=" km"></Span>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                                <Label IsVisible="{Binding Zvijezde.HasValue}"  LineBreakMode="NoWrap" 
                                Style="{DynamicResource ListItemTextStyle}" 
                                FontSize="13" >
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span>Ocjena korisnika:  </Span>
                                            <Span Text="{Binding Zvijezde}"></Span>
                                            <Span Text="/5"></Span>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Odjavi se" Clicked="Button_Clicked"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>